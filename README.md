# albert

This package contains R functions useful for Public Health Informatics work.

## Installation

This package depends on the core Tidyverse packages, so they will also be installed alongside this package. Install using:

```{r}
remotes::install_github("hennepin-county-public-health/albert")
```
